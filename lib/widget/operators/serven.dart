import 'package:flutter/material.dart';

class ServenButton extends StatelessWidget {
  const ServenButton({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text(
      '7',
      style: TextStyle(
        // backgroundColor: Colors.amber,
        fontSize: 30,
      ),
    );
  }
}
