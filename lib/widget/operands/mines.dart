import 'package:flutter/material.dart';

class MinesButton extends StatelessWidget {
  const MinesButton({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text(
      '-',
      style: TextStyle(
        // backgroundColor: Colors.amber,
        fontSize: 30,
      ),
    );
  }
}
