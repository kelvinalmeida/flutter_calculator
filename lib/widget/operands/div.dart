import 'package:flutter/material.dart';

class DivButton extends StatelessWidget {
  const DivButton({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text(
      '/',
      style: TextStyle(
        // backgroundColor: Colors.amber,
        fontSize: 30,
      ),
    );
  }
}
